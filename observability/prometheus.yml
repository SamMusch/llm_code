

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Scrape OTEL Collector Prometheus exporter
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]

  # Optional: Tempo metrics (disabled by default unless you expose metrics)
  # - job_name: tempo
  #   static_configs:
  #     - targets: ["tempo:3200"]

  # Optional: Loki metrics (disabled by default unless you expose metrics)
  # - job_name: loki
  #   static_configs:
  #     - targets: ["loki:3100"]